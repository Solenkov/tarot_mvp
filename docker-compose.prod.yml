services:
  app:
    build: ./backend
    container_name: tarot_mvp_app
    restart: unless-stopped
    environment:
      - PORT=8000
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    ports:
      - "127.0.0.1:8000:8000"
